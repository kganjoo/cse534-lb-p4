
port map --> port:<server, isAssigned>

state 0 
    1. server = 0
    
first packet of every request --> check the syn bit
if(syn bit set )
    --> invalidate previous entry ( set server index and port map to 0 )
    
//read server index, read is assigned

if(isAssigned == 0)
    1. if state  = 0
        isAssigned = 1
        serverIndex = 1
    2. if state = 1
        isAssigned = 1
        serevrIndex = hash
        
 
        
        
    
